
/**
 * Enum containing Vote type constants.
 */

export enum VoteType {

    /**
     * vote radio type.
     * @type {number}
     */
    RADIO = 1,

    /**
     * vote multiple type.
     * @type {number}
     */
    MULTIPLE = 0
}
